#pragma once

// Global flag controlling whether the emulator is allowed to enter sleep / power-save.
// When true, CPU sleep instructions and LCD power-save are disabled.
extern bool g_disableSleep;
